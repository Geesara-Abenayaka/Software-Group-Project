name: Node.js CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4

    # 1. Build the Frontend (at the root)
    - name: Install Frontend Dependencies & Build
      run: |
        npm ci
        npm run build --if-present

    # 2. Setup and Restart Backend (inside the server directory)
    - name: Setup Backend Environment
      working-directory: ./server
      run: |
        npm ci
        # Create the .env file exactly where the server.js expects it
        touch .env
        echo "MONGODB_URI=${{ secrets.MONGODB_URI }}" >> .env
        echo "PORT=${{ secrets.PORT }}" >> .env
        echo "DB_USERNAME=${{ secrets.DB_USERNAME }}" >> .env
        echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" >> .env
        echo "SENDGRID_API_KEY=${{ secrets.SENDGRID_API_KEY }}" >> .env
        echo "SENDGRID_FROM_EMAIL=${{ secrets.SENDGRID_FROM_EMAIL }}" >> .env
        echo "SENDGRID_FROM_NAME=${{ secrets.SENDGRID_FROM_NAME }}" >> .env
        echo "SENDGRID_REPLY_TO=${{ secrets.SENDGRID_REPLY_TO }}" >> .env
        echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> .env
        echo "GEMINI_VISION_MODEL=${{ secrets.GEMINI_VISION_MODEL }}" >> .env
        echo "ALLOW_TESSERACT_FALLBACK=${{ secrets.ALLOW_TESSERACT_FALLBACK }}" >> .env

    # 3. Restart PM2 using the correct process name
    - name: Deploy with PM2
      run: pm2 restart server --update-env
